# import math
# num = int(input('Digite um número: '))
# raiz = math.sqrt(num)
# print('A raiz de {} é igual a {}'.format(num, raiz))
# No código a cima, o comando "import" importou toda a biblioteca de matemática.


# No código abaixo, foi feita apenas as importações das funcionalidades específicas, não de toda a biblioteca.
# Caso seja necessário mais funcionalidades, deve ser acrescentado separando por vírgula.
# from math import sqrt, floor, ceil
# num = int(input('Digite um número: '))
# raiz = sqrt(num)
# print('A raiz de {} é igual a {:.2f}.'.format(num, raiz))
# print('Utilizando o arredondamento para baixo {}'.format(floor(raiz)))
# print('utilizando o arredondamento para cima {}'.format(ceil(raiz)))

# No código abaixo importaremos a biblioteca "random" para gerar números aleatórios.
# import random
# Gera automaticamente números entre 0 e 1, para números inteiros digite dentro do parênteses o intervalo: "random.randint(1, 10)"
# num = random.randint(1, 3)
# print(num)

# Para instalar pacotes externos, como o emoji, vá em File, Settings, Project, Python Interpreter, clicar no sinal de "+" para adicionar
# digite o nome do pacote, neste caso "emoji" e clica em instalar.
#import emoji
#print(emoji.emojize("Olá, Mundo :earth_americas:", language='alias'))

